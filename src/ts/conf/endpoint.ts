// ホストドメイン
export const HOST_NAME = 'localhost';
export const PORT = '5000';
export const DOMAIN_NAME = `http://${HOST_NAME}:${PORT}`;

// --- エンドポイント ---
// メッセージ送信
export const E_SEND_MESSAGE = 'send-message';
